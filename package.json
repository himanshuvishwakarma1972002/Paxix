{
  "name": "paix-website-frontend",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/paix-website run dev",
    "build": "pnpm --filter @workspace/paix-website run build",
    "serve": "pnpm --filter @workspace/paix-website run serve",
    "typecheck": "pnpm --filter @workspace/paix-website run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
